# ARGOS (in meters)

A_WIDTH = 3.0
A_HEIGHT = 3.0

A_MAX_X =  A_HEIGHT / 2
A_MIN_X = -A_MAX_X
A_MAX_Y =  A_WIDTH / 2
A_MIN_Y = -A_MAX_Y

# REAL

# to argos coordinates
function to_argos(point){

}

# from argos coordinates
function from_argos(point){

}
